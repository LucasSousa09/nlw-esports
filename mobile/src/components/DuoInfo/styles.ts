import { StyleSheet } from 'react-native';
import { THEME } from '../../theme';

export const styles = StyleSheet.create({
  container: {
    marginBottom: 16,
  },
  label: {
    color: '#C4C4C6',
    marginBottom: 4,
  },
  info: {
    fontWeight: 'bold',
  },
});